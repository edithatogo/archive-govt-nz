# Treasury capture plan

The current 54-dataset Treasury inventory contains 91 resources. This plan is
metadata-only: no payload bytes were downloaded. Each resource has an explicit
policy decision in the paired JSON manifest, and unknown transport/type facts
remain fail-closed until bounded preflight and streaming capture provide them.
