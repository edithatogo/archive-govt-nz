# Specification

Adopt the byte-versioned medallion v1 JSON Schema vocabulary shared with
`reimbursement-atlas` and `global-medicines-atlas`. Keep archive runtime models
local while CI verifies layer names, checksums, lineage, rights decisions,
promotion status, complete gates, adjacent transitions, and fail-closed behavior.
