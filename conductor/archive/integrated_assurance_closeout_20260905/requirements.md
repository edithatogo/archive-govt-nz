# Requirements

## Must

- Bind all assurance claims to the exact integrated target commit and archived donor identity.
- Run the complete repository harness and exact-head hosted gates without weakening thresholds.
- Execute every supplementary legislation integrity mutation suite in a clean environment, including parent restoration, durable state, seed, configuration, and publication readback.
- Preserve failures, environment differences, command logs, receipt hashes, and independent review.
- Verify reproducible packages, bounded real-state verification resources, security alerts, and enforced main ruleset.
- Keep supplemental hosted execution explicitly opt-in, read-only, secret-free, timeout-bounded, and limited to existing suites.
- Add a superseding final receipt and leave Prompt 20 incomplete until every required gate passes.

## Should

- Reuse existing hosted runner and action versions and separate the harness fix from the evidence commit.

## Could

- Reuse exact earlier operational run timing as supplementary context without claiming current-head benchmarking.

## Will not

- Add production features, publish datasets, dispatch harvests, or modify immutable external records.
