# Scope

Replace Actions state restoration only. A trusted, versioned JSON parent reference pins live metadata and state roots. A separate authority record permits explicit bootstrap or adoption of an otherwise verified legacy package. No authority or live parent is fabricated by this issue.
