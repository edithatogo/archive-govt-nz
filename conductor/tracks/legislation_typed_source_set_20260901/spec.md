# Specification

Replace both legislation configuration parsers with one versioned JSON-Schema-validated typed YAML loader. Inventory and enforce all current legislation fields plus explicit execution, scope, authority, rights, preservation, publication/gates, limits and concurrency semantics. Preserve legacy migration only for the known v1 legislation shape; keep non-legislation legacy capture compatible without expanding this issue. No scheduling, state merge, publication or coverage analysis.
