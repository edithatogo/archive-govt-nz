# Requirements

## Must

- Verify all 68 donor batch files and the normalized 33,693-candidate universe from exact bytes.
- Keep candidate, target-governed reviewed, attempted, retrieved, work, expression, manifestation, normalized, CAS, published, metadata-only and terminal-disposition populations distinct.
- Bind every known count to a primary artifact and SHA-256; represent unproven counts as unknown rather than zero.
- Produce deterministic per-batch and aggregate JSON/Markdown reports, a mismatch ledger and a Prompt 17 correction manifest.
- Reject overlap, duplicate IDs, hash drift, missing evidence and semantic population collapse.

## Should

- Record current public Hugging Face and Zenodo observations separately from local state and historical snapshots.

## Excluded

- Rewriting closeout/evaluator documents, Hub cards, Zenodo metadata, publication, or changes to the independent legislation product.
