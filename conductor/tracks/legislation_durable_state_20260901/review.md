# Self-review

Scope is Prompt 09 only. Checked the input pin, original-file inventory, merged
parent descriptors/archives, native checkpoint/CAS validation, Prompt 08 sealed
continuations, canonical ZIP representation, exclusive outputs, quarantine
promotion, rights-safe metadata projection and publication-plan truth fields.
No workflow, canonical source state, public dataset card or independent product
was edited. No platform/dependency change or remote publisher was introduced.

The caller authenticates input pins and publication approval; package fields do
not confer authority. Preserved parent ZIPs are opaque, exact previously verified
originals. Canonical encoding is a second archive defense; the final negative
tests require early name/type/duplicate rejection rather than relying only on
that later check. The first campaign's three surviving guard removals and all
failed fixture/lint attempts remain in quarantine and will be hashed at closeout.

The real pinned canonical merge produced 71,776,346 bytes and restored all 561
original files, with 552 work IDs/records/objects. Repeated builds matched exactly.
This is local implementation validation, not independent Prompt 10 acceptance.
The payload is blocked, and its plan contains only a minimal metadata candidate;
published revision and DOI remain null and remote readback remains false.

HF authority is selected prospectively within the existing identity. Git history
and hashes do not guarantee availability or prevent owner deletion. Gating does
not authorize redistribution. No immutable GitHub release is asserted, and the
observed Zenodo concept/version distinction is a recorded handoff. Remote
publication and final independent recovery require their separate gates.

Final native assurance, the strengthened mutation campaign and exact-head hosted
checks must pass before delivery. Optional external review is not claimed.

## Superseding bounds review

Two additional negative tests showed that verification needed to enforce the
shared restorer's expanded-state limit, and package-file reads needed to reject
non-regular inputs before open. Both are now rejected by explicit preflight
controls. The reviewed helper has 190 executable statements and 24 branches;
44 focused tests cover all of them. Dedicated controls bring the final mutation
campaign to 26. The superseding canonical package preserves all original state
bytes; only builder revision/hash metadata changes. The previous package and
receipts remain retained. Final full and hosted assurance remain delivery gates.
