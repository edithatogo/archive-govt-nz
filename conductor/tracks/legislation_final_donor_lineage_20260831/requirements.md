# Requirements

- MUST-1: verify live donor archival and exact target/donor commits against audited donor baseline.
- MUST-2: inventory all 15 audited late commits and any observed drift; each gets exactly one disposition.
- MUST-3: import the final tree byte-for-byte and preserve prior snapshot; verify Git tree and per-file SHA-256.
- MUST-4: account for PR/issue sequence 170–184, hosted runs, failures, claims, workflow routes, capabilities and identity lineage.
- MUST-5: bind counts, omissions and exact target import commit; verify full local and hosted gates before merge.
- SHOULD: preserve reproducible focused verification instructions and immutable source patches.
- COULD: help the controller route later specialist work once its prompt texts exist.
