# Fail-closed legislation workflow correction

Replace merged scheduled orchestration claims with sequence-gated,
dispatch-only workflows backed by the corrected service and authenticated
cumulative state. No workflow is run by this local track.
