# Run Log: Standards and Schema Conformance

- Enriched `schemas/legislation/v2/legislation-record.schema.json` with relationships, provenance, and uncertainty metadata.
- Validated strict conformance of v1 and v2 schemas using `tools/validate_schemas.py`.
- Tested JSON Schema Draft 2020-12 runtime validation in `validate_legislation_record`.
