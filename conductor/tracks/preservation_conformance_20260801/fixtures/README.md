# Conformance fixture corpus

This fixture is intentionally small and synthetic. It is not Treasury payload
material and cannot establish production-corpus conformance. Consumers should
verify the hashes in `manifest.json` before evaluating a package.
