# Retained Crown expenses: metadata-only candidate scope

This is a bounded archival design inventory, not a selected denominator, source
capture, workbook-cell verification, new package or rights decision.

## Strongest next candidates

1. BEFU 2026 core Crown expense workbook, source `befu_2026-003`, SHA256
   `313ee040abd9a332cc36245da5a0c2cb0d38fe2cedc013d731c1f12db463b0d1`,
   193830 bytes. Existing reviewed `forecast-successors.md` establishes sheet
   `Core Crown Expense Tables`, year cells F5:O5 (2021–2030), type cells
   F6:O6 (2021–2025 Actual; 2026–2030 Forecast), unit D6 `($millions)`.
   Existing Health-only adapter does not establish total-expense coordinates.
2. HYEFU 2025 corresponding workbook, source `hyefu_2025-006`, SHA256
   `f9f9190a69ce0a7c53b89d690d50170e2a6a457e6821afeb93edc08cef4b9c7d`,
   191581 bytes. Existing reviewed contract establishes the same sheet name,
   years F4:O4, types F5:O5, unit D5 `($millions)` and same supplied year/type
   sequence. Again, a total-row coordinate or literal/formula state is not
   established by the selected Health profile.
3. Historical fiscal indicators 1972–2025, `fiscal_time_series-007`, SHA256
   `de59f9028a81a697ee66eea04861edfd8e2c3a7e472b3b8798d976951964f70f`.
   Existing historical adapter selects Health in `Spending` plus nominal GDP;
   it is not evidence that all total/core Crown expense series have the same
   period/accounting basis or numeric carrier.

Exact official URLs and observed-at timestamps already exist in the repository
source census. These links were not fetched. Recorded source licensing metadata
is not a fresh eligibility grant for any new derivative.

## Proposed smallest future profile (not implementation approval)

Prefer one explicitly labeled core Crown total row within one exact forecast
vintage, only after exact label, coordinates, numeric tokens/formulas and
surrounding definition/footnote dependencies are independently reviewed. Keep
source-native Actual/Forecast and vintage distinctions. Retain a literal or
cached value only under an explicitly reviewed carrier policy; existing sheets
contain 400 formulas each, currently preserved only. Do not compute a total from
component rows, infer a formula result, or equate core Crown with total Crown.
Do not splice vintages, infer financial-year dates, silently select NZD from a
dollar symbol, or select an expenditure denominator. Preserve all excluded cells
and complete per-cell dispositions with no changes to existing Health outputs.

## Remaining exact-cell blocker

Spreadsheet skill and required API/style instructions were read. The official
workspace dependency loader reports: `No bundled workspace runtime dependencies
are configured yet.` No runtime was guessed, installed or substituted for a
spreadsheet action. Existing reviewed archival code/contracts were read only;
new expense labels, exact row locations, formula state and footnotes remain
unverified. Next bounded step is an approved retained archival inspection of
those exact candidates (or provision of the configured spreadsheet runtime),
then a reviewed literal profile and red-first tests. No new acquisition needed.
